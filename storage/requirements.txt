# =============================================================================
# ATLAS Analytics Container — Python Dependencies
# =============================================================================
# Pinned for reproducibility. Installed at build time (not runtime).
# =============================================================================

clickhouse-connect>=0.7.0,<1.0
psycopg[binary]>=3.1,<4.0
pyarrow>=14.0,<18.0
pandas>=2.1,<3.0
deltalake>=0.14,<1.0
plotly>=5.20,<6.0
streamlit>=1.34,<2.0
psycopg2-binary>=2.9.9