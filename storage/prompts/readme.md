the prompts defined here: [Prompts Directory](prompts/) will be used by the loaded SLM model for summarising the stuff.
To load the prompt , just paste your prompt into rca_system_prompt.txt and the model will use it to generate the RCA. The prompt can be modified to suit your needs, but make sure to keep the format intact.
