# =============================================================================
# ATLAS ML Inference Pipeline — Python Dependencies
# =============================================================================
# Owner: Knsrikanta (ML Inference)
#
# Install locally:
#   pip install -r requirements.txt
#
# These are kept deliberately minimal — only what inference.py needs.
# =============================================================================

# Data processing
pandas>=2.1.0
numpy>=1.26.0

# ML — must match the version used during training to avoid pickle errors
scikit-learn>=1.3.0

# Parquet I/O
pyarrow>=14.0.0
