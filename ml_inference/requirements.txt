# =============================================================================
# ATLAS ML Inference Pipeline — Python Dependencies
# =============================================================================
# Owner: S Nandini (ML Inference)
#
# Install locally:
#   pip install -r requirements.txt
#
# These are kept deliberately minimal — only what inference.py needs.
# =============================================================================

# Data processing
pandas>=2.1.0
numpy>=1.26.0

# ML — must match the version used during training to ensure joblib compatibility
scikit-learn>=1.3.0

# Model serialisation — used by train_model.py (Sanjula) and inference.py (S Nandini)
# Must be present: joblib.load() is used for isolation_forest.pkl,
# preprocessor.pkl, and health_score_config.pkl
joblib>=1.3.0

# Parquet I/O
pyarrow>=14.0.0
