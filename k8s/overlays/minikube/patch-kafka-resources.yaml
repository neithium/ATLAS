apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: atlas-kafka
  namespace: atlas
spec:
  template:
    spec:
      containers:
        - name: kafka
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1536Mi"
